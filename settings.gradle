include ':app'
include ':router-annotations'
include ':router-processor'
include ':biz-reading'
include ':router-runtime'

rootProject.name = "MyRouter"

gradle.taskGraph.beforeTask { task ->
    println("[all tasks] >>> ${task.name}")
}
